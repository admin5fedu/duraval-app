export const MAX_LINKS_PER_ITEM = 3
export const DEFAULT_ITEMS_COUNT = 10
export const MAX_CONSECUTIVE_ERRORS = 3

export const DEBOUNCE_DELAY_MS = 1500 // Auto-save delay (reduced from 3s to 1.5s for better UX)
export const STATUS_UPDATE_DELAY_MS = 500 // Status text debounce
export const TYPING_TIMEOUT_MS = 2000 // Typing timeout (reduced from 5s to 2s to match debounce better)
export const RETRY_DELAY_MS = 2000 // Retry delay when save fails
export const MAX_RETRY_ATTEMPTS = 3 // Maximum retry attempts

